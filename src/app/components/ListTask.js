import React from 'react'
import { Container} from 'react-bootstrap'

const ListTask = ({ year, month, day }) => {
    return <Container>
        <h3>Sin tareas registradas</h3>
    </Container>
}

export default ListTask;